# Base URL of the backend API (FastAPI/Flask service exposing /api/v1/*)
NEXT_PUBLIC_API=http://localhost:8000

# When "true", the UI reads all data from lib/mockAPI.ts instead of calling the backend.
# Useful for local development, demos, or when the backend is not running.
NEXT_PUBLIC_MOCK=false
